/*
n = 8, A = 37, 10, 22, 30, 35, 13, 25, 24

array part(n, *Arrptr)
{
    new array1[n/2] <- A[0] ~ A[n/2]
    new array2[n/2] <- A[n/2 + 1] ~ A[n]

    return both;
}

array merge(n, *Arrptr1, *Arrptr2)
{
    new array[n*2] <- 
}
*/